<%= form_for(@mobile) do |f| %>
  <% if @mobile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mobile.errors.count, "error") %> prohibited this mobile from being saved:</h2>

      <ul>
      <% @mobile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :clicked_at %><br />
    <%= f.datetime_select :clicked_at %>
  </div>
  <div class="field">
    <%= f.label :mobile_count %><br />
    <%= f.text_field :mobile_count %>
  </div>
  <div class="field">
    <%= f.label :total_count %><br />
    <%= f.text_field :total_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
